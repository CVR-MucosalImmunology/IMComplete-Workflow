# metadata.yaml (example)
project_name: # imput from the newProj function
options:
  denoise: true
  extra: 

# function: extract
pass: true

# check setup
pass: true #if pass
fail: {collection of packages not installed}
discrep: {packages with differing versions}

# 


analysis_params:
  gaussian_sigma: 1.5
notes: |
  This file stores the current analysis parameters. 
  Historical data is kept in .metadata folder.